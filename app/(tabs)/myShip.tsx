import { Text, View } from "react-native";

export default function MyShip() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>My Ship</Text>
    </View>
  );
}
